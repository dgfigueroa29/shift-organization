rootProject.name = "shift-organization"

include(
    "shared",
    "lambda-properties",
    "lambda-bookings",
    "lambda-recurring-events",
    "lambda-notifications",
    "lambda-health",
    "lambda-recurring-processor"
)
