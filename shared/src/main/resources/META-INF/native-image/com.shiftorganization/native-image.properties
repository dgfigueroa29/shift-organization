# GraalVM native image configuration for Shift Organization shared module.
# Applied automatically when the native-image tool processes this jar.
Args = --initialize-at-build-time=org.slf4j \
       --initialize-at-build-time=ch.qos.logback \
       -H:+ReportExceptionStackTraces \
       --no-fallback
